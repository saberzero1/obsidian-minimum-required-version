{
  "name": "obsidian-minimum-required-version-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r run typecheck",
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "extract-registry": "tsx scripts/extract-registry.ts"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.0",
    "js-yaml": "^4.1.0",
    "obsidian": "^1.7.2",
    "prettier": "^3.4.0",
    "publint": "^0.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.15.4"
}
